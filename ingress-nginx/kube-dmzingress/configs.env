log-format-upstream="$http_x_forwarded_for" $remote_user [$time_local] "$request" $request_time $host $body_bytes_sent $request_time $status "$http_referer" "$http_user_agent" "$remote_addr" $cookie_WSPE_SID $cookie_CoreID6 $cookie_WSPFY $cookie_svi_dec $http_akamai_grn $namespace
#log-format-upstream="$http_x_forwarded_for" $remote_user [$time_local] "$request" $request_time $host $body_bytes_sent $request_time $status "$http_referer" "$http_user_agent" "$remote_addr" $cookie_WSPE_SID $cookie_CoreID6 $cookie_WSPFY $cookie_svi_dec $http_akamai_grn $namespace $request_body $http_debug
server-tokens=false
upstream-keepalive-timeout=300
worker-shutdown-timeout=330s