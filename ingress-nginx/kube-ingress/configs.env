log-format-upstream="$http_x_forwarded_for" $remote_user [$time_local] "$request" $request_time $host $body_bytes_sent $request_time $status "$http_referer" "$http_user_agent" "$remote_addr" $cookie_WSPE_SID $cookie_CoreID6 $cookie_WSPFY $cookie_svi_dec $http_wsi_trace_id $namespace
#log-format-upstream="$http_x_forwarded_for" $remote_user [$time_local] "$request" $request_time $host $body_bytes_sent $request_time $status "$http_referer" "$http_user_agent" "$remote_addr" $cookie_WSPE_SID $cookie_CoreID6 $cookie_WSPFY $cookie_svi_dec $http_wsi_trace_id $namespace $request_body $http_debug